# [RAY] EAR RULING — humanize round LOCKED IN (2026-08-16)

Ray A-B'd the 2026-08-15 listen set (`listen/2026-08-15-humanize/` — title, stage, waltz,
panic; before = 7a0cc5c engine, after = the humanized engine + Popinjay overrides +
SCORE_PERFORMANCE): **"after is better across the board, lock them in."**

The humanized performance engine (room reverb 3.4s/2.6 decay, fade-in/out, breathing voice
envelopes, seeded performance humanization via SCORE_PERFORMANCE) is RATIFIED as the game's
score rendering for all four pieces. The pre-humanize render is superseded as a sound target;
the listen set stays as the comparison record. No further score-engine ear gate is pending —
future score changes ride the normal Song-Structure/register checks.
