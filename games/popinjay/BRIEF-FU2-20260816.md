# Follow-up fix round 2 — popinjay, 2026-08-16

Three MINOR findings from the 2026-08-16 fix-wave verdict remain open. Fix all three.

## 1. Notice slot collides with the REHEARSAL banner

`src/render/overlays.js:282` — `const y = HUD_H + 4` (native y 26) is the new controller-notice
slot; `drawControllerNotice` paints native rows 26..56 there. `src/render/overlays.js:233`
`drawRehearsal` calls `band(p, 24, 30)` with instruction text at y 29/41 — the exact same band.
A pad connecting during REHEARSAL now paints the opaque notice card over the rehearsal
instruction text.

Fix: reposition one of the two so their y-ranges don't overlap during REHEARSAL. Either move the
controller-notice slot below/above the rehearsal band specifically while REHEARSAL is active, or
have `drawRehearsal` yield/skip its own band while a controller notice is showing. Your call which
is cleaner — just eliminate the overlap.

## 2. Climbing player near the top of a tower can still be occluded

The fix that solved the GROUNDED-player occlusion (notice moved to native y 26..56) proved zero
overlap for `playerHead=256, GROUND=277`. But the central tower top sits around native y~41 in the
captures — inside the new 26..56 band. A pad connect firing mid-climb reproduces the same class of
defect in a rarer pose (connect is not gated to a "safe" moment).

Fix: give the notice card alpha-fade (partial transparency) OR reroute it to a corner toast that
never overlaps any playable vertical range. Verify specifically against a mid-climb pose near tower
top, not just the grounded pose the original test covered.

## 3. J-collision still freezes KeyJ itself

`src/app.js:489-490` — Climb-up and Climb-down are both bound to `KeyJ`. Evaluating UP then DOWN in
one pass nets zero cursor motion when J is pressed (verified: `{key:'j', before:3, after:3,
moved:false}`). The keyboard menu-recovery fix (this session's MAJOR) already gives players a way
out via Reset Defaults (reachable via ArrowDown), but J itself stays dead as bound.

Fix: add a duplicate-binding guard — if two reserved actions share one key in the same evaluation
pass, either resolve which one wins deterministically, or refuse the duplicate binding at bind time
so it can't happen. Don't just silently drop one direction; make the behavior explicit and tested.

## Verification duties

- Run the full test battery (`npm test`) — must be 100% green. Baseline was 276/0/0/0 at last
  harvest; note your before/after counts in the report.
- If you touch or add tests: no assertion deletion, no loosening, no skip/only/todo. Strengthen or
  add — never weaken. If a fenced constant would need to move to make a test pass, don't move it —
  flag it instead.
- Re-run the two-overlay collision probe style used in the prior verdict (native-pixel y-range
  overlap check) for items 1 and 2 — a visual "looks fine" is not sufficient, measure it.

## Do NOT commit or push. Leave changes in the working tree.

Write a summary of what you changed and verified to `LANE-REPORT-FU2.md` at repo root.
