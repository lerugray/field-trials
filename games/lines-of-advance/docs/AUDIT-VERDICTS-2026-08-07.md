# Seed-audit verdicts (2026-08-07, canonical Hammerstein via opus)

All six findings verdict-gated REAL and applied:
1. M1 gate made falsifiable (source doc + >=90% cited rows Boolean); rules-bearing milestones gated behind it. No rules source exists on this machine tonight — acquisition is an open item (Atlas Press 2007 English ed. carries rules+board; ask operator whether he owns it; bookfinder-general otherwise).
2. M2 reordered FIRST (zero rules dependency) so the builder is never schedule-pressured into rubber-stamping the ledger.
3. M5 acceptance = depth-N beats depth-N-1 over >=20 self-play games + node count + PV display. Horizon (multi-turn comms attrition) named as the real engine risk, not raw perf.
4. IP checklist must name exact unit values/counts/setup coordinates as the contested class — an explicit operator decision, not laundered under "mechanics". ATTRIBUTION.md narrowed to idea-expression framing.
5. "Faithful" banned from player-facing surfaces unless the verified fraction is stated alongside (folded into rule 2).
6. Checkpoint-commit rule kept but treated as best-effort: the ORCHESTRATOR runs a read-only diff watchdog + fixed harvest order (snapshot -> WIP-commit as found -> test) on every lane in this repo.

Prior-art sweep of existing digital implementations = rules CROSS-CHECK only, never a thing to copy.
