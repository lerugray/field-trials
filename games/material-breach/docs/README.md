# docs/

- `DIRECTIONS-<date>-<topic>.md` — **operator directives. They outrank DESIGN-SEED.md.** The
  builder reads the newest one at the start of every run.
- `proofs/<YYYY-MM-DD>-M<n>/` — dated proof screenshots. A milestone with no proof image is not
  closed (AGENTS.md hard rule 10).
- `REGISTER-SEED.md` — authored at M0 from DESIGN-SEED §4.1, with cited exemplars.
